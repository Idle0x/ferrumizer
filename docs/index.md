# ferrumizer

**Gradients through the furnace.**

Ferrumizer composes a thermal history, carbon diffusion, and hardening model into one differentiable heat-treatment pipeline. It reports hardness traverses and effective case depth at 550 HV.

Start with `make data`, then `ferrumize verify`.
